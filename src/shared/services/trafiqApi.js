// Stub API service — replace with real fetch calls when backend is ready
export const trafiqApi = {
    async getEvents() {
        return { ok: false }; // Use mock data from useTrafikData
    },
    async getRoutesStatus() {
        return { ok: false };
    },
};
