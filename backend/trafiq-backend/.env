# Database (MongoDB)
MONGODB_URI=mongodb://localhost:27017/trafiq_db

# JWT
JWT_SECRET=trafiq_super_secret_jwt_key_2024
JWT_EXPIRES_IN=7d

# Email (Mailhog local dev catcher — UI at http://localhost:8025)
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USER=
MAIL_PASS=
MAIL_FROM=no-reply@trafiq.tn

# App
APP_URL=http://localhost:3000
